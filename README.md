# IEP Management System

Система управления индивидуальными образовательными планами (IEP) с адаптивным обучением и автоматической генерацией заданий с использованием AI.

## 🚀 Быстрый старт

### Предварительные требования

Убедитесь, что у вас установлены:
- [Git](https://git-scm.com/downloads)
- [Docker](https://www.docker.com/get-started) и Docker Compose
- [Node.js](https://nodejs.org/) (v18+) - если запускаете frontend локально
- [Python](https://www.python.org/) (3.11+) - если запускаете backend локально

### 1. Клонирование репозитория

```bash
git clone https://github.com/sunset2209/DD_mvp1.git
cd DD_mvp1
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите необходимые параметры:

```env
# Обязательно! Получите API ключ на https://openrouter.ai
OPENROUTER_API_KEY=your-openrouter-api-key

# Опционально (для production)
SECRET_KEY=your-super-secret-key-change-in-production
```

### 3. Запуск через Docker (рекомендуется)

Самый простой способ запустить все сервисы:

```bash
docker-compose up --build
```

Приложение будет доступно по адресам:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **PostgreSQL**: localhost:5432

Для остановки:
```bash
docker-compose down
```

Для остановки с удалением данных:
```bash
docker-compose down -v
```

### 4. Запуск локально (без Docker)

#### Backend

```bash
# Установите зависимости
pip install -r requirements.txt

# Убедитесь, что PostgreSQL запущен и доступен
# Обновите DATABASE_URL в .env если нужно

# Примените миграции
alembic upgrade head

# Создайте таблицы (если нужно)
python create_tables.py

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend

```bash
cd frontend

# Установите зависимости
npm install

# Создайте .env.local (если нужно изменить API URL)
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local

# Запустите dev сервер
npm run dev
```

## 📁 Структура проекта

```
.
├── app/                    # Backend (FastAPI)
│   ├── api/               # API endpoints
│   │   └── v1/           # API version 1
│   ├── core/             # Настройки безопасности
│   ├── models/           # SQLAlchemy модели
│   ├── schemas/          # Pydantic схемы
│   ├── services/         # Бизнес-логика
│   │   └── generation/  # AI генерация заданий
│   ├── config.py         # Конфигурация
│   └── main.py           # Точка входа
├── frontend/              # Frontend (Next.js)
│   ├── app/              # Next.js app router
│   ├── components/       # React компоненты
│   └── lib/              # Утилиты
├── tests/                 # Тесты
├── alembic/              # Миграции БД
├── docker-compose.yml    # Docker конфигурация
└── requirements.txt      # Python зависимости
```

## 🔑 Основные функции

- **Управление учениками**: создание и редактирование профилей
- **IEP (Индивидуальные образовательные планы)**: создание персонализированных планов
- **AI генерация заданий**: автоматическое создание заданий на основе целей IEP
- **Отслеживание прогресса**: мониторинг выполнения заданий
- **Аутентификация**: JWT-based авторизация
- **Многопользовательская система**: поддержка организаций и пользователей

## 🛠 Технологии

**Backend:**
- FastAPI (Python)
- PostgreSQL + SQLAlchemy
- Alembic (миграции)
- JWT Auth
- OpenRouter API (LLM)

**Frontend:**
- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- React Query

## 📝 API Документация

После запуска backend, документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🧪 Тестирование

```bash
# Запуск тестов
pytest

# С покрытием
pytest --cov=app tests/

# Конкретный тест
pytest tests/test_auth.py
```

## 🔧 Разработка

### Применение миграций

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "описание изменений"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1
```

### Линтинг и форматирование

```bash
# Python (Ruff)
ruff check app/
ruff format app/

# TypeScript/JavaScript
cd frontend
npm run lint
```

## 🐛 Решение проблем

### Порты уже заняты

Если порты 3000, 8000 или 5432 уже используются, измените их в `docker-compose.yml`:

```yaml
services:
  app:
    ports:
      - "8001:8000"  # Изменить внешний порт
```

### Ошибка подключения к базе данных

Убедитесь, что:
1. PostgreSQL запущен
2. `DATABASE_URL` в `.env` корректен
3. База данных `adaptive_learning` существует

### Ошибки AI генерации

Проверьте:
1. `OPENROUTER_API_KEY` установлен в `.env`
2. У вас есть кредиты на OpenRouter
3. Модель `anthropic/claude-3-haiku` доступна

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 👥 Контакты

GitHub: https://github.com/sunset2209/DD_mvp1
